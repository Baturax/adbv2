# tool for adb
make sure tk and virtualenv is installed  (in windows it should come installed, in linux you can install it using your package manager. iepacman -S python-virtualenv)

python -m venv venv

# linux
and then, source adb/bin/activate
in fish source adb/bin/activate.fish

# windows 
env/Scripts/activate.bat //In CMD
env/Scripts/Activate.ps1 //In Powershel
