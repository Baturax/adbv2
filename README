# tool for adb
make sure wget, tk, android-file-transfer and virtualenv is installed  (in linux you can install it using your package manager. ie pacman -S python3-virtualenv python3-tkinter wget android-file-transfer)

python -m venv venv

# linuxvenv
and then, 
source venv/bin/activate
in fish, source venv/bin/activate.fish
